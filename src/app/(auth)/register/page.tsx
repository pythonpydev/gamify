import { RegisterForm } from '@/components/auth';

export const metadata = {
  title: 'Create Account | Pokerdoro',
  description: 'Create your Pokerdoro account and start earning chips',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
