import { RegisterForm } from '@/components/auth';

export const metadata = {
  title: 'Create Account | Study Poker',
  description: 'Create your Study Poker account and start earning chips',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
